import {
    React,
    fireEvent,
    render,
    screen,
  } from "../../../../global/test-imports";
  
describe('Pagination', () => {
    test('should ', () => {
      expect(1).toBe(1)
    });
});